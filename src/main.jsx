import React from 'react';
import ReactDOM from 'react-dom/client';
import LessonSwitcher from './LessonSwitcher.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LessonSwitcher />
  </React.StrictMode>
);
